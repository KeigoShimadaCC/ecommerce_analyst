# Deviation Log

Append an entry whenever a doc, process, gate, or scope deviation occurs. Each entry must include timestamp, phase, what happened, what doc change would have prevented it, and severity.

- 2026-06-10 16:21:28 JST - Phase 01 - The first P1 orchestrator run emitted no new events for roughly five minutes while the harness and subagent child processes were near-zero CPU during the first worker handoff. Per the stall policy, the Lead terminated only the P1 PIDs, restored the partial working-tree edits to the committed baseline, and will relaunch the identical brief once. Preventive doc change: none currently known; this matches the documented transient model-call hang policy. Severity: medium process deviation.
- 2026-06-10 18:25:30 JST - Phase 02 - The first Lead-owned normal app-path SDK proof succeeded structurally but exposed a chart-scale defect: generated money chart values were dollar-scale while persistence rendered them as `currency_cents`, producing about `$24` instead of about `$2,366`. Preventive doc change: require the Phase 02 brief and result contract tests to assert integer-cent chart values for money metrics, plus an explicit live-proof scale check comparing answer prose, highlights, chart/table rendering, and generated `result.json` values before accepting the engine. Severity: high evidence-integrity deviation.
- 2026-06-10 18:48:00 JST - Phase 03 - The supervisor compressed the final scope and explicitly authorized the Lead to stop the active P3 orchestrator and finish directly so the repo would not remain unpushed. Preventive doc change: add a documented final-hour emergency path that allows Lead-owned direct edits only for submission-critical finish work, with mandatory phase report and deviation evidence. Severity: medium process deviation.
